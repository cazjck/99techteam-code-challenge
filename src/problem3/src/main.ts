import {
    sumToNWithIterative,
    sumToNWithFormula,
    sumToNWithRecursive
} from "./sumToN";

// Testing sumToNWithIterative
try {
    console.log(sumToNWithIterative(5)); // Output: 15
} catch (error) {
    console.error(error);
}

// Testing sumToNWithFormula
try {
    console.log(sumToNWithFormula(5)); // Output: 15
} catch (error) {
    console.error(error);
}

// Testing sumToNWithRecursive
try {
    console.log(sumToNWithRecursive(5)); // Output: 15
} catch (error) {
    console.error(error);
}